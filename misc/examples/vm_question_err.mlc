// ? operator Err path.
type Result<T, E> = Ok(T) | Err(E)

fn load() -> i32!i32 = Err(3)

fn run() -> i32!i32 = do
  let value = load()?
  Ok(value)
end

fn main() -> i32 = do
  match run() {
    Ok(_) => 0,
    Err(e) => e + 4
  }
end
