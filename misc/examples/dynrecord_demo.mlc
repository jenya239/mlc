//! DynRecord import smoke (TRACK_EXAMPLES_CI STEP=7).
//! Proves relative stdlib imports compile under mlcc. Field access via
//! Symbol literals (`:name`) is not exercised here.

import { DynRecord, dyn_new } from '../../lib/mlc/common/stdlib/collections/dynrecord.mlc'

fn main() -> i32 = do
  let _record: DynRecord = dyn_new()
  0
end
