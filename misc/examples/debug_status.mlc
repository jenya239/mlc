fn normalize(line: str) -> str = line.trim()

fn parse_status(line: str) -> str =
  const trimmed = normalize(line).upper()
  if trimmed.starts_with("TODO") then
    "TODO"
  else if trimmed.starts_with("DOING") then
    "DOING"
  else if trimmed.starts_with("DONE") then
    "DONE"
  else
    "OTHER"

fn main() -> i32 =
  const _ = println(parse_status("TODO: something"))
  const _ = println(parse_status("DOING: now"))
  const _ = println(parse_status("DONE: done"))
  0
