# TRACK_VM_BLOCK_ID_COLLISION: elif fallthrough must not hang the VM.
fn classify(value: i32) -> i32 =
  if value == 0 then 100
  else if value == 1 then 200
  else 300

fn main() -> i32 = do
  classify(5)
end
