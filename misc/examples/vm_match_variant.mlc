// Variant match via Choice-typed helper.
type Choice = Left(i32) | Right(i32)

fn choose(value: Choice) -> i32 =
  match value {
    Left(x) => x,
    Right(y) => y
  }

fn main() -> i32 = choose(Left(3))
