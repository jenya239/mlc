// ? operator Ok path.
type Result<T, E> = Ok(T) | Err(E)

fn load() -> i32!i32 = Ok(7)

fn run() -> i32!i32 = do
  let value = load()?
  Ok(value)
end

fn main() -> i32 = do
  match run() {
    Ok(x) => x,
    Err(_) => 0
  }
end
