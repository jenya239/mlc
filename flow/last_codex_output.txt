Added SimpleCov to Minitest bootstrap so coverage starts automatically for test runs with HTML + Cobertura formatters, gracefully warning if the gem isn’t available (`test/test_helper.rb`). Tests were not re-run after this change. Next step: install `simplecov`/`simplecov-cobertura` in the sandbox and run `rake test_fast` to confirm coverage initialization works end-to-end.

Added SimpleCov to Minitest bootstrap so coverage starts automatically for test runs with HTML + Cobertura formatters, gracefully warning if the gem isn’t available (`test/test_helper.rb`). Tests were not re-run after this change. Next step: install `simplecov`/`simplecov-cobertura` in the sandbox and run `rake test_fast` to confirm coverage initialization works end-to-end.