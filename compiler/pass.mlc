// Compiler pass abstraction (PLAN.md §3). Pipeline execution: pass_manager.mlc + pipeline.mlc.

import { Program, Result, Diagnostic } from './frontend/ast'

export trait CompilerPass<Input, Output> {
  fn run(self: CompilerPass<Input, Output>, input: Input) -> Result<Output, [Diagnostic]>
}