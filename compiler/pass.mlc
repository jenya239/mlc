// Compiler pass abstraction (PLAN.md §3). Not wired into main.mlc yet.

import { Program, Result, Diagnostic } from './ast'

export trait CompilerPass<Input, Output> {
  fn run(self: CompilerPass<Input, Output>, input: Input) -> Result<Output, [Diagnostic]>
}

export type CheckerPass = CompilerPass<Program, Program>
