// TRACK_CONCURRENCY_FFI_METADATA STEP=2 —
// thread_affine(MainThread) extern fn inside spawn → error[E094].
extern fn gui_paint() -> unit = "gui_paint" from "<stdio.h>" thread_affine(MainThread)

fn main() -> unit = do
  let _task = spawn do
    gui_paint()
    return ()
  end
  ()
end
