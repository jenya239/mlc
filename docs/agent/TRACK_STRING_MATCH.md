# Track: String match patterns (Phase 2.5 §1)

Parent: [../PLAN.md](../PLAN.md) §Phase 2.5; next: [TRACK_DESTRUCTURING_APPLY.md](TRACK_DESTRUCTURING_APPLY.md)

## Status: **open** (step 1 pending)

**Goal:** ???????? ? ???? `match` ?? ????????? ?????????. ????????? ??? `if/else if word == '...'` ??????? ? compiler/ (~5 ??????, 150+ ?????).

```mlc
// ??:
fn keyword_kind(word: string) -> TokenKind =
  if      word == 'fn'   then KFn
  else if word == 'type' then KType
  ...

// ?????:
fn keyword_kind(word: string) -> TokenKind =
  match word {
  | 'fn'   -> KFn
  | 'type' -> KType
  | _      -> KIdentifier(word)
  }
```

## Verify gate (every step)

```
compiler/tests/build_tests.sh
compiler/build.sh
compiler/out/mlcc -o .tmp_selfhost/p1 compiler/main.mlc
compiler/build_bin.sh .tmp_selfhost/p1 .tmp_selfhost/mlcc2
.tmp_selfhost/mlcc2 -o .tmp_selfhost/p2 compiler/main.mlc
diff -rq .tmp_selfhost/p1 .tmp_selfhost/p2   # empty
```

---

| Step | Item | Status |
|------|------|--------|
| 1 | Parser: `StringLitPattern` ? AST (`frontend/ast.mlc`) — ????? ??????? `PatStringLit(string)` | pending |
| 2 | Parser: ?????? ?????????? ???????? ??? ???????? ? match-???? (`parser/exprs.mlc`) | pending |
| 3 | Checker: ??? subject `string` ? OK; ????????? ? E-???????????; `_` ?????????? | pending |
| 4 | Codegen string: string match ? `if/else if` ??????? ? C++ | pending |
| 5 | Codegen CppAST: string match ? `CppIfElse` chain | pending |
| 6 | ?????: `test_parser.mlc` + `test_checker.mlc` + `test_decl_gen.mlc` ??? string match | pending |
| 7 | ????????? ? `frontend/lexer.mlc` — `keyword_kind`, `map_escape`, `scan_op` | pending |
| 8 | ????????? ? `cpp/lexer.mlc` — `cpp_keyword_kind`, `cpp_scan_op` | pending |
| 9 | ????????? ? `checker/semantic_type_structure.mlc` — dispatch ?? ????? ?????? | pending |
| 10 | ????????? ? `codegen/expr/method_gen.mlc` + `compile_options.mlc` | pending |
| 11 | Audit: ??? `if.*==.*'` ? compiler/ ????? ????????? ????????? | pending |
| 12 | ??????? ???? | pending |

## Sub-steps (step 1 detail)

- `frontend/ast.mlc`: ???????? `PatStringLit(string)` ? `type Pattern`
- `frontend/ast.mlc`: ???????? `pat_span` ??? ?????? ????????
- `compiler/tests/test_parser.mlc`: ???? ?? `match s { | 'x' -> 1 | _ -> 0 }`
