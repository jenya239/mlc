# mlcc architecture

???????: **??????? = ??????? ????? ???????????????, ??????, ???????????? ? ?????????** � ?? enterprise-???? ? ??????????? ?? ?????? ???????.

?????????: MLIR (multi-level IR + progressive lowering), rustc (queries), LLVM NPM (analysis vs transform), Nanopass (?????? passes + verifiers). ??? premature MLIR/LLVM dependency.

---

## IR ladder (target)

```text
source
  ? Tokens                    frontend/lexer
  ? AST                       frontend/ast, parser/
  ? HIR / resolved AST        checker/names, registry  (today: ???????? ? check)
  ? Typed IR                  SemanticIR + TypeRegistry checker/infer, transform/
  ? MLC MIR                     mir/ — flat CFG between SemanticIR and CppAST (TRACK_MIR)
  ? CppAST                    cpp/cpp_ast, codegen/
  ? C++ text                  cpp printer / emit
```

**???????:** ??????? N ?? ????? ??? emission. Backend ?? ??????????? parser.

### ?????????? (verifier ????? ??????? ???????? pass)

| ????? | ????????? |
|-------|-----------|
| parse | well-formed AST, spans ?? ???? ????? |
| resolve/names | ??? unresolved idents ? scope |
| typecheck | ? ??????? expr ???? ??? ??? ????? error |
| transform ? SemanticIR | ??? sugar, trait/destugar ???????? |
| mir | explicit control flow, no nested expr side effects |
| CppAST | well-formed C++ AST nodes |
| emit | golden/diff ???????? |

---

## ??? ???? ??????

```text
main.mlc          driver + loader + imports + fmt + lsp + cpp headers  ? ???????
pipeline.mlc      check ? transform ? codegen                         ? 3 ????, ok
CompilerPass      trait ????????, ?? wired
SemanticIR        typed AST — codegen today; target: MIR then CppAST
codegen/module    ????????? transform + build_registry              ? ?????????
checker/check     ????? ??? codegen (record_defaults acceptable_for_codegen)
```

AST → SemanticIR → MirProgram → CppAST — target pipeline. Legacy `ir/core.mlc` removed (MIR).

---

## Pass architecture

```text
Pass = fn(state, input) -> Result<output, Diagnostic>
Analysis = pure fn(ir) -> facts   (?????????? pass manager'?? ?????)
Transform = mutates / produces new IR
```

**??????:** ????????? ????????? passes (infer, transform, codegen). **????:** ????? PassManager + preserved-analyses ???????? (LLVM-style, ??????????).

Nanopass-??scipline: API ????? ?????? passes; production ????? fuse hot passes (batch lowering).

### Analysis vs transform (?? ?????????)

```text
analysis:  cfg, dominance, use-def, type facts, escape
transform: const fold, dce, inlining, closure conv, lowering
```

Pass ??????????? analysis, ?? ????????????? ???.

---

## Driver vs core

```text
driver/     CLI, File I/O, compile_commands, LSP adapter, profile
core/       lexer � codegen (??? File.*)
```

LSP/format � adapters ??? **queries** (??????? ????): `diagnostics_of(source)`, `type_at(span)`.

---

## Data-oriented core (hot paths)

```text
NodeId / TypeId / SymbolId   opaque types (Phase 2.7)
arena / compact Vec            ?????? Shared<T> ??? ????????
intern strings               ?????, ????
??????? dispatch             visitor ?????? ?? ???????? IR, ?? ?? ?????? ????
```

Frontend/diagnostics � ????????? ??stractions. Typechecker/codegen � ??????? ?????????.

---

## Observability (???????????)

```text
--dump-tokens / --dump-ast / --dump-sem / --dump-core / --dump-cpp
--time-passes
--verify-each
--trace-pass=<name>
```

Premature observability > premature abstraction.

### Code quality gates

Lint: `compiler/tests/run_architecture_lint.sh` (IR imports, file size, survivor trends).
Allowlist: `compiler/tests/architecture_lint_allowlist.txt` � shrink over time.
Details: [archive/tracks/TRACK_CLEAN_ARCHITECTURE.md](archive/tracks/TRACK_CLEAN_ARCHITECTURE.md) �Code quality gates.

---

## Testing strategy

| ??????? | ??? |
|---------|-----|
| unit | parser, type rules, single-pass lowering |
| verifier | invalid IR ? must fail verify_* |
| golden | snapshot ????? pass (--dump-* output) |
| differential | mlcc vs Ruby checker (????) |
| fuzz | parser, lowering, optimizer (????) |
| integration | pipeline check-only, mlcc2 diff |

Layout: `tests/unit/{syntax,hir,types,core,cpp}/`, `tests/integration/`, `tests/e2e/`.

---

## ?? ??????

```text
IExpressionVisitor + INodeFactory + ITypeResolver ?? ?????? ????
entities / repositories / controllers
typechecker ? codegen ?????? ?????
parser ????? ??? backend
pass ?????? ?????????? singleton
??????????? ???????? AST ??? verifier
```

---

## Roadmap tracks

| Track | ?????????? |
|-------|------------|
| [TRACK_CLEAN_ARCHITECTURE](archive/tracks/TRACK_CLEAN_ARCHITECTURE.md) | verifiers, pass manager, driver split, tests, dumps |
| Phase 2.7 | opaque IDs, `?`/guard |
| [TRACK_MIR](archive/tracks/TRACK_MIR.md) | MLC MIR types, verifier, lowering center |
| Future: TRACK_CORE_IR | deprecated — superseded by TRACK_MIR |
| Future: TRACK_QUERY_ENGINE | demand-driven queries — see [QUERY_ENGINE.md](QUERY_ENGINE.md) |
| Future: TRACK_INCREMENTAL | persistent CST / stable node ids |

??. [PLAN.md](PLAN.md) �Phase 2.8.
