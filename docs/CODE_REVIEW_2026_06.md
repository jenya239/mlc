# Code Review � mlcc compiler (June 2026)

?????? ???? 138 ?????? / 31k ?????. ?????? ????: `frontend/`, `checker/`, `codegen/`, `cpp/`.

---

## 1. ?????????????? ??????????? ?????

### Record destructuring � 0 ????????????? ? ???????????
???? ???????????? `let { x, y } = record` (???? ? `test_checker.mlc:364`), ?? ?? ???? ?????? ??????????? ??? ?? ?????????.

```mlc
// ?????? � ????? ???:
let result = cpp_scan_ident(state)
tokens.push(result.tok)
state = result.st

// ?????? ????:
let { st, tok } = cpp_scan_ident(state)
tokens.push(tok); state = st
```

?????????: ???? `cpp/lexer.mlc`, `frontend/lexer.mlc`, `codegen/`, `checker/`.

### .map() / .fold() / .filter() � ????? ?? ???????????? ? codegen
HOF ??? ???????? ? ????? ???? ? ???????????? ? HOF-checker ? ??????, ?? `codegen/` ? `cpp_printer.mlc` ????? ?????????? `while index < arr.length() do ... push ... end`.

```mlc
// ?????? � cpp_printer.mlc, 7 ????:
let mut parts: [string] = []
let mut i = 0
while i < params.length() do parts.push(print_param(params[i])); i = i + 1 end
parts.join(', ')

// ?????? ????:
params.map(p => print_param(p)).join(', ')
```

### match ?? ADT ?????? if/else if
? `checker/check.mlc` ??? ?????????? ????? ? ????? ?????, ??????? ????? ??????????:
```mlc
// ??????:
match variant {
| VarUnit(n,_)     -> set(n, true)
| VarTuple(n,_,_)  -> set(n, true)
| VarRecord(n,_,_) -> set(n, true)
}
// ???? ???? ????????? or-????????:
match variant {
| VarUnit(n,_) | VarTuple(n,_,_) | VarRecord(n,_,_) -> set(n, true)
}
```

---

## 2. ??????????? ??????????? ?????

### Match ?? ??????? (?????????: HIGH)
??? ??????? keyword-dispatch � ??????? if/else if. ????????? ~5 ??????, 150+ ?????.

```mlc
// ?????? � frontend/lexer.mlc:58�87 (28 ?????):
if      word == 'fn'    then KFn
else if word == 'type'  then KType
...

// ?????:
match word {
| 'fn'   -> KFn
| 'type' -> KType
| _      -> KIdent(word)
}
```

????? ? parser + checker + codegen: string-???????? ? match-?????.

### ?????????? ParseResult<T>
15 ???????? ? ??????????? ?????? ? `parser/predicates.mlc`:
```mlc
type ExprResult = ExprResult { value: Expr,  parser: Parser }
type PatResult  = PatResult  { value: Pat,   parser: Parser }
type TypeResult = TypeResult { value: TypeExpr, parser: Parser }
// ...? ??? 12

// ????? (????? generics ?? ?????? record):
type ParseResult<T> = ParseResult { value: T, parser: Parser }
```

### Type aliases
`cpp_ast.mlc` ???????? 17� `[Shared<CppType>]`, `[Shared<CppExpr>]` ? ??????????:
```mlc
// ?????:
type CppExprs  = [Shared<CppExpr>]
type CppStmts  = [Shared<CppStmt>]
type CppParams = [Shared<CppParam>]
```

### ????????? / scan_while
??? scan-??????? ? ????? ???????? ?????????:
```mlc
while !current.eof() && pred(current.current()) do current = current.advance() end
// ?????: let current = state.scan_while(is_alnum)
```

### Closures � ? ???????? ??? Result
`Result<T,E>` ???????? ? `ast.mlc`, ?? ????? ?? ????????????. ?????? � ?????? ?????? ? sentinel-????????.

### for item in collection
??? ???????? � ?????? ????????? ?????. `for` ??? ???? ? ????? ?????????????, ?? ?? ???????????? ? ???????????.

---

## 3. ????????????? ????????

| ??????? | ???-?? ????? | ????? |
|---------|-------------|-------|
| keyword dispatch (if/else if ?? ??????) | 50+ ????? | lexer.mlc, semantic_type_structure.mlc, method_gen.mlc |
| expect_rbrace/rparen/rbracket | 6 | parser/exprs.mlc, parser/decls.mlc |
| comma_separated_*_from_first | 4 | types.mlc, decls.mlc, exprs.mlc |
| precedence-????? `while go` | 5 | parser/exprs.mlc |
| named-arg reorder | 2 | infer_call.mlc, transform.mlc |
| generic substitution build | 3 | infer_match, transform, let_pattern_infer |
| HOF method spec (map/fold/zip) | 2�N | infer_array_method, transform |
| print_comma_separated + while-push-join | 4 + 7 | cpp_printer.mlc |
| is_X() predicates | 53 + 20 | parser/predicates.mlc, cpp/predicates.mlc |
| visit-arm helpers ? void_return | 4 | cpp_printer.mlc |

---

## 4. ??? / ????????????? (??. ????????? ?????? ????)

?????????? ??????: ~22 `extend` ?? 138 ??????. ???????? ????? ???? � ????????? ??????? ???? `fn gen_expr(context, expr)`. ????????? ??? ???????? ? ??????? ??????? � ??. ?????? 5.

---

## 5. ????????????? ????????

### ??? ???????????? backend (HIGH)
`codegen/` ???????? ?????? string-???? ? CppAST-????. Phase 2 ????? ????????? � ????? ??????? string-backend.

### ??????? ???????
- `transform/transform.mlc` � 828 ?????, `transform_expr` ~290 ?????
- `infer/infer.mlc` � 592 ??????
- `check/check.mlc` � 483 ??????

????? ??????? ?? ????? AST ??? ??????? ? `infer_call.mlc`, `infer_match.mlc` ? ?.?.

### TypeRegistry � god-object
15+ map ? ????? ????????? (`fn types`, ctors, fields, traits, assoc types). ????? ???????????? `FnIndex`, `AdtIndex`, `RecordIndex`.

### ???: StmtLet ?????? StmtLetConst
`parse_statement_const` ??????? `StmtLet` ?????? `StmtLetConst` (`parser/exprs.mlc:214�228`).

### cpp/parser.mlc ?????? ? string
`cpp/parser.mlc` ?????? ??? ??? `type_string: string`, ?? ??? `CppType`. ?????? ? printer.

### exprs.mlc ??????????? lexer
`build_template_expr` ???????? `tokenize` � ????????? ???? ????????????.

---

## 6. ??????????

| # | ?????? | Impact | Effort |
|---|--------|--------|--------|
| 1 | Match ?? ??????? � ???????? ? ???? | high | medium |
| 2 | ????????? record destructuring ?? ????? ??????????? | high | low |
| 3 | .map().join() ?????? while-push ? cpp_printer + codegen | high | low |
| 4 | ??????? string-backend ????? ???????? Phase 2 | high | medium |
| 5 | ????? named-args + substitution (infer+transform) | medium | medium |
| 6 | join_mapped + ?????????? visit-arm ? cpp_printer | medium | low |
| 7 | Type aliases ? ???? | medium | medium |
| 8 | ??????? transform.mlc | medium | medium |
| 9 | ParseResult<T> ????? generics | medium | high |
| 10 | fix StmtLetConst bug | medium | low |
