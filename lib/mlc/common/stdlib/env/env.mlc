//! @module std/env/env
//! @category env
//! @type extern
//! @namespace mlc::env

# Process environment (TRACK_STDLIB_ENV_LOGGING).
# Missing key → None / default — not last_error.

module Env

export extern fn get(key: string) -> Option<string>
export extern fn get_or(key: string, default: string) -> string
export extern fn has(key: string) -> bool
