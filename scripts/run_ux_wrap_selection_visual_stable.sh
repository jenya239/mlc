#!/usr/bin/env bash
# TRACK_EDITOR_WRAP_SELECTION_VISUAL — green after STEP=2.
set -euo pipefail

echo "[ux wrap_selection_visual_stable] FAIL: wrap selection visual not implemented (expected red until STEP=2)" >&2
exit 1
