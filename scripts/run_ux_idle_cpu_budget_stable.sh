#!/usr/bin/env bash
# TRACK_EDITOR_IDLE_CPU_BUDGET — live idle CPU% under budget (green after STEP=2).
set -euo pipefail

echo "[ux idle_cpu_budget_stable] FAIL: live sample not implemented (expected red until STEP=2)" >&2
exit 1
