# Optional force-skip basenames for TRACK_EXAMPLES_CI soft-skip allowlist.
# One basename per line (e.g. experimental_demo.mlc).
#
# dynrecord_demo.mlc — mlcc segfaults compiling dynrecord.mlc graph (Option/
# Map extern); import paths fixed, body deferred (TRACK_EXAMPLES_CI STEP=7).
dynrecord_demo.mlc
